# Core -- required
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
PyYAML>=6.0

# The Claude-written narrative. Without this (or without ANTHROPIC_API_KEY)
# the system writes reports locally instead -- it never goes silent.
anthropic>=0.40

# Optional
# openpyxl>=3.1        # reading .xlsx exports
# transformers>=4.40   # swap the lexicon sentiment for a small local model
# torch>=2.0           # ditto
